kind: pipeline
type: docker
name: workshop-demo-app

platform:
  os: linux
  arch: amd64

trigger:
  branch:
  - develop
  event:
  - push

steps:
- name: pytest2
  image: python
  commands:
  - pip install -r requirements.txt
  - pytest --cov-report xml --cov-report term --cov ./lib/